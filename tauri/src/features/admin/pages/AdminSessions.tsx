// AdminSessions.tsx — Thin wrapper around shared SessionChat component
import { SessionChat } from '../../../components/SessionChat'

export default function AdminSessions() {
  return <SessionChat layout="full" />
}
